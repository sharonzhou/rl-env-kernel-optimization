Default prompt template includes RAG (or RAG is a tool)

MI355 prompt template

Prompt constructor iterates through open-source models, available in SGLang and vLLM

Please make a separate directory `output/` and make kernel optimizations and modifications there.